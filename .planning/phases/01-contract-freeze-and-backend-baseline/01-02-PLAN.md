---
phase: 01-contract-freeze-and-backend-baseline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [server/app.js, server/server.js, server/lib/runtimeConfig.js, server/lib/apiResponder.js, docs/backend-separation/backend-runtime-target.md]
autonomous: true
requirements: [ARCH-01, ARCH-02, API-02]
user_setup: []
must_haves:
  truths:
    - "The backend has a documented target runtime shape separate from the old single-entry bootstrap."
    - "Shared backend concerns are prepared for reuse instead of being duplicated per route/runtime."
  artifacts:
    - path: "server/app.js"
      provides: "Reusable Express app bootstrap separated from listen()"
      min_lines: 40
    - path: "server/lib/runtimeConfig.js"
      provides: "Central runtime/env parsing for backend bootstrap"
      min_lines: 20
    - path: "docs/backend-separation/backend-runtime-target.md"
      provides: "Source-of-truth backend target contract"
      min_lines: 40
  key_links:
    - from: "server/server.js"
      to: "server/app.js"
      via: "server entrypoint imports reusable app"
      pattern: "from './app\\.js'"
    - from: "server/app.js"
      to: "server/lib/runtimeConfig.js"
      via: "centralized runtime config"
      pattern: "runtimeConfig|createRuntimeConfig"
---

<objective>
Establish the reusable backend foundation for the separation program.

Purpose: Make Express a proper application boundary that can be reused across local server startup, future adapters, and deployment runtimes without duplicating bootstrap logic.
Output: Reusable `server/app.js`, centralized runtime helpers, and backend target documentation.
</objective>

<execution_context>
@C:/Users/Dipanshu pandey/OneDrive/Desktop/Office Work/indiantrademartmain-frontback-main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dipanshu pandey/OneDrive/Desktop/Office Work/indiantrademartmain-frontback-main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-contract-freeze-and-backend-baseline/01-CONTEXT.md
@.planning/phases/01-contract-freeze-and-backend-baseline/01-api-surface-inventory.md
@server/server.js
@server/lib/auth.js
@server/lib/supabaseClient.js
@server/lib/httpSecurity.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extract reusable Express bootstrap and runtime helpers</name>
  <files>server/app.js, server/server.js, server/lib/runtimeConfig.js, server/lib/apiResponder.js</files>
  <read_first>server/server.js, server/lib/httpSecurity.js, server/lib/auth.js, server/lib/supabaseClient.js</read_first>
  <action>Create `server/app.js` that builds and exports the configured Express app without calling `listen()`. Move env parsing and runtime defaults into `server/lib/runtimeConfig.js`, and add a small responder helper in `server/lib/apiResponder.js` for consistent JSON/error output where practical. Update `server/server.js` so it imports the app and starts listening using the centralized runtime config.</action>
  <verify>rg -n "export .*app|createRuntimeConfig|from './app.js'" server/app.js server/lib/runtimeConfig.js server/server.js</verify>
  <acceptance_criteria>
    - `server/app.js` exports the configured Express app
    - `server/server.js` imports `./app.js` instead of building the app inline
    - runtime/env defaults are centralized in `server/lib/runtimeConfig.js`
  </acceptance_criteria>
  <done>The backend can be bootstrapped through a reusable app module instead of a single hard-wired entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Document the backend target contract</name>
  <files>docs/backend-separation/backend-runtime-target.md</files>
  <read_first>.planning/phases/01-contract-freeze-and-backend-baseline/01-api-surface-inventory.md, .planning/phases/01-contract-freeze-and-backend-baseline/01-netlify-compat-matrix.md, server/app.js, server/server.js</read_first>
  <action>Write a backend target document describing runtime boundaries, server ownership, Netlify adapter intent, auth/session responsibilities, environment variables, and what "backend proper" means for this repo. Include a section defining what remains temporarily compatible versus what must be migrated in Phase 2.</action>
  <verify>rg -n "## Runtime Ownership|## Adapter Strategy|## Auth and Session Boundary" docs/backend-separation/backend-runtime-target.md</verify>
  <acceptance_criteria>
    - the document names Express as the primary API runtime
    - the document explains Netlify compatibility behavior during migration
    - the document lists env/runtime responsibilities for local and deployed execution
  </acceptance_criteria>
  <done>The migration has a written backend target that future plans can execute against.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `node -e "import('./server/app.js').then(m => console.log(Boolean(m.default || m.app))).catch(() => process.exit(1))"` succeeds
- [ ] `rg -n "from './app.js'" server/server.js` returns a match
- [ ] `rg -n "## Runtime Ownership|## Adapter Strategy" docs/backend-separation/backend-runtime-target.md` returns matches
</verification>

<success_criteria>

- All tasks completed
- Express bootstrap becomes reusable
- Backend target documentation matches the actual repo migration strategy
  </success_criteria>

<output>
After completion, create `.planning/phases/01-contract-freeze-and-backend-baseline/01-02-SUMMARY.md`
</output>
