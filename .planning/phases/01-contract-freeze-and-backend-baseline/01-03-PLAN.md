---
phase: 01-contract-freeze-and-backend-baseline
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [tester-playwright-live/tests/parity/api-contract-smoke.spec.mjs, tester-playwright-live/fixtures/api-contract-baseline.json, tester-playwright-live/README.md, package.json]
autonomous: true
requirements: [TEST-01, API-02]
user_setup: []
must_haves:
  truths:
    - "The migration has a repeatable regression baseline for public and authenticated API-critical flows."
    - "A parity manifest exists so later phases can verify they did not silently drop key endpoints or responses."
  artifacts:
    - path: "tester-playwright-live/tests/parity/api-contract-smoke.spec.mjs"
      provides: "Live-safe parity smoke coverage"
      min_lines: 40
    - path: "tester-playwright-live/fixtures/api-contract-baseline.json"
      provides: "Named parity baseline for backend migration"
      min_lines: 20
  key_links:
    - from: "tester-playwright-live/tests/parity/api-contract-smoke.spec.mjs"
      to: "tester-playwright-live/fixtures/api-contract-baseline.json"
      via: "test-driven parity manifest"
      pattern: "api-contract-baseline"
---

<objective>
Create the verification baseline for backend separation.

Purpose: Give later migration phases a concrete, rerunnable way to prove that critical API-backed behavior still works after ownership moves to Express.
Output: A parity manifest, Playwright smoke coverage for contract-sensitive flows, and updated test documentation/scripts.
</objective>

<execution_context>
@C:/Users/Dipanshu pandey/OneDrive/Desktop/Office Work/indiantrademartmain-frontback-main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dipanshu pandey/OneDrive/Desktop/Office Work/indiantrademartmain-frontback-main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-contract-freeze-and-backend-baseline/01-CONTEXT.md
@.planning/phases/01-contract-freeze-and-backend-baseline/01-api-surface-inventory.md
@tester-playwright-live/README.md
@tester-playwright-live/playwright.config.mjs
@tester-playwright-live/tests/public/home-and-footer.spec.mjs
@tester-playwright-live/tests/authenticated/vendor-regression.spec.mjs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add API parity manifest and smoke test</name>
  <files>tester-playwright-live/fixtures/api-contract-baseline.json, tester-playwright-live/tests/parity/api-contract-smoke.spec.mjs</files>
  <read_first>.planning/phases/01-contract-freeze-and-backend-baseline/01-api-surface-inventory.md, tester-playwright-live/playwright.config.mjs, tester-playwright-live/tests/public/home-and-footer.spec.mjs</read_first>
  <action>Create a small JSON baseline listing critical endpoints, expected status ranges, and user journeys that must survive the migration. Add a Playwright smoke test that reads the manifest and validates at least public health/catalog reachability plus one authenticated API-backed portal flow without mutating production data.</action>
  <verify>rg -n "criticalEndpoints|authenticatedFlows|publicFlows" tester-playwright-live/fixtures/api-contract-baseline.json && rg -n "api-contract-baseline|authenticatedFlows" tester-playwright-live/tests/parity/api-contract-smoke.spec.mjs</verify>
  <acceptance_criteria>
    - the baseline JSON names public and authenticated contract-sensitive flows
    - the new spec reads the baseline file instead of hard-coding all cases inline
    - the new spec avoids live data mutation
  </acceptance_criteria>
  <done>The migration has a parity smoke test driven by a baseline manifest.</done>
</task>

<task type="auto">
  <name>Task 2: Update test entrypoints and documentation</name>
  <files>tester-playwright-live/README.md, package.json</files>
  <read_first>tester-playwright-live/README.md, package.json, tester-playwright-live/package.json</read_first>
  <action>Document the new parity suite in `tester-playwright-live/README.md` and add a root-level helper script in `package.json` that makes parity verification discoverable from the main repo. Keep the existing live-safe testing language and do not imply mutation coverage unless explicitly enabled.</action>
  <verify>rg -n "parity|api contract" tester-playwright-live/README.md package.json</verify>
  <acceptance_criteria>
    - the README mentions when to run parity checks during backend migration
    - the root package exposes a clearly named parity script or alias
    - documentation stays consistent with live-safe test constraints
  </acceptance_criteria>
  <done>The team can reliably run the baseline verification before and after migration steps.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "criticalEndpoints|authenticatedFlows" tester-playwright-live/fixtures/api-contract-baseline.json` returns matches
- [ ] `rg -n "api-contract-baseline" tester-playwright-live/tests/parity/api-contract-smoke.spec.mjs` returns a match
- [ ] `rg -n "parity|api contract" tester-playwright-live/README.md package.json` returns matches
</verification>

<success_criteria>

- All tasks completed
- A parity manifest exists for backend migration
- Playwright documentation and repo scripts expose the baseline check clearly
  </success_criteria>

<output>
After completion, create `.planning/phases/01-contract-freeze-and-backend-baseline/01-03-SUMMARY.md`
</output>
