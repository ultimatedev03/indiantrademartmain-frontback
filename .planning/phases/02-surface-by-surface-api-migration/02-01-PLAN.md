---
phase: 02-surface-by-surface-api-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [server/lib/auth.js, server/routes/auth.js, netlify/functions/auth.js, src/lib/customSupabaseClient.js, src/modules/buyer/context/AuthContext.jsx, src/modules/vendor/context/AuthContext.jsx, src/modules/admin/context/InternalAuthContext.jsx, src/modules/employee/context/EmployeeAuthContext.jsx]
autonomous: true
requirements: [ARCH-04, API-03, API-04, AUTH-01, AUTH-02]
user_setup: []
must_haves:
  truths:
    - "All main portals continue to authenticate through the same cookie + CSRF contract after backend auth migration."
    - "Auth logic has one primary implementation path, with compatibility layers only where explicitly required."
  artifacts:
    - path: "server/routes/auth.js"
      provides: "Primary auth route implementation"
      min_lines: 200
    - path: "src/lib/customSupabaseClient.js"
      provides: "Browser auth shim aligned to backend session contract"
      min_lines: 120
  key_links:
    - from: "src/lib/customSupabaseClient.js"
      to: "/api/auth/me"
      via: "session refresh bridge"
      pattern: "/api/auth/me"
    - from: "netlify/functions/auth.js"
      to: "server/lib/auth.js"
      via: "shared auth behavior or adapter reuse"
      pattern: "server/lib/auth|shared"
---

<objective>
Consolidate auth and shared session behavior behind the backend-first contract.

Purpose: Every later migration step depends on stable auth cookies, CSRF validation, role resolution, and portal session behavior.
Output: A single primary auth implementation path plus aligned frontend auth shims and contexts.
</objective>

<execution_context>
@C:/Users/Dipanshu pandey/OneDrive/Desktop/Office Work/indiantrademartmain-frontback-main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dipanshu pandey/OneDrive/Desktop/Office Work/indiantrademartmain-frontback-main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-surface-by-surface-api-migration/02-CONTEXT.md
@docs/backend-separation/backend-runtime-target.md
@server/lib/auth.js
@server/routes/auth.js
@netlify/functions/auth.js
@src/lib/customSupabaseClient.js
@src/modules/buyer/context/AuthContext.jsx
@src/modules/vendor/context/AuthContext.jsx
@src/modules/admin/context/InternalAuthContext.jsx
@src/modules/employee/context/EmployeeAuthContext.jsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Make Express auth the primary implementation path</name>
  <files>server/lib/auth.js, server/routes/auth.js, netlify/functions/auth.js</files>
  <read_first>server/lib/auth.js, server/routes/auth.js, netlify/functions/auth.js, docs/backend-separation/backend-runtime-target.md</read_first>
  <action>Extract or reuse shared auth helpers so `server/routes/auth.js` remains the source of truth for login, register, me, buyer-profile, password, and logout behavior. Reduce `netlify/functions/auth.js` duplication by delegating to shared server logic or a thin compatibility adapter instead of maintaining a fully separate auth code path.</action>
  <verify>rg -n "login|register|buyer/profile|logout" server/routes/auth.js && rg -n "server/lib/auth|adapter|shared" netlify/functions/auth.js</verify>
  <acceptance_criteria>
    - Express auth remains the canonical implementation path
    - Netlify auth no longer carries a fully separate business-logic fork without shared reuse
    - shared auth helpers remain responsible for cookies, CSRF, role normalization, and password handling
  </acceptance_criteria>
  <done>Backend auth ownership is consolidated without changing portal behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Align browser auth shim and portal contexts to the backend contract</name>
  <files>src/lib/customSupabaseClient.js, src/modules/buyer/context/AuthContext.jsx, src/modules/vendor/context/AuthContext.jsx, src/modules/admin/context/InternalAuthContext.jsx, src/modules/employee/context/EmployeeAuthContext.jsx</files>
  <read_first>src/lib/customSupabaseClient.js, src/modules/buyer/context/AuthContext.jsx, src/modules/vendor/context/AuthContext.jsx, src/modules/admin/context/InternalAuthContext.jsx, src/modules/employee/context/EmployeeAuthContext.jsx, server/routes/auth.js</read_first>
  <action>Update the browser auth shim and portal auth contexts so they rely on the backend session contract consistently, remove stale fallback assumptions where possible, and keep role mismatch handling, logout propagation, and session refresh behavior aligned across buyer, vendor, admin, and employee surfaces.</action>
  <verify>rg -n "/api/auth|SIGNED_IN|SIGNED_OUT|refreshSession" src/lib/customSupabaseClient.js src/modules/buyer/context/AuthContext.jsx src/modules/vendor/context/AuthContext.jsx src/modules/admin/context/InternalAuthContext.jsx src/modules/employee/context/EmployeeAuthContext.jsx</verify>
  <acceptance_criteria>
    - all listed contexts rely on the backend-backed auth shim rather than ad hoc auth state logic
    - role mismatch and logout behavior stay centralized through `customSupabaseClient.js`
    - no portal-specific auth context silently bypasses the backend session contract
  </acceptance_criteria>
  <done>Portal auth flows stay compatible while moving to a backend-first implementation.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "/api/auth" src/lib/customSupabaseClient.js` returns matches
- [ ] `rg -n "server/lib/auth|shared|adapter" netlify/functions/auth.js` returns matches
- [ ] auth contexts still reference the shared auth shim rather than direct session duplication
</verification>

<success_criteria>

- All tasks completed
- Auth contract remains stable for all portals
- Primary auth logic is backend-owned and reusable
  </success_criteria>

<output>
After completion, create `.planning/phases/02-surface-by-surface-api-migration/02-01-SUMMARY.md`
</output>
