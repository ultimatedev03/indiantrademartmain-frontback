---
phase: 03-cutover-cleanup-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/apiBase.js, vite.config.js, tools/devAll.js, package.json, .env.netlify.example, README.md]
autonomous: true
requirements: [DEP-01, DEP-02, OPS-01]
user_setup: []
must_haves:
  truths:
    - "Frontend and backend have a clear runtime contract for local and deployed environments."
    - "The browser points at the backend boundary intentionally instead of relying on mixed legacy assumptions."
  artifacts:
    - path: "src/lib/apiBase.js"
      provides: "Final browser API base behavior"
      min_lines: 20
    - path: "tools/devAll.js"
      provides: "Local startup flow reflecting separated backend runtime"
      min_lines: 30
    - path: "README.md"
      provides: "Updated local setup guidance for separated runtime"
      min_lines: 40
  key_links:
    - from: "vite.config.js"
      to: "src/lib/apiBase.js"
      via: "dev proxy aligns with browser API base behavior"
      pattern: "proxy|/api"
    - from: "tools/devAll.js"
      to: "server/server.js"
      via: "backend startup in local dev"
      pattern: "server/server\\.js"
---

<objective>
Finalize the runtime contract for frontend/backend cutover.

Purpose: A separated backend is only real when local dev, build scripts, env files, and browser API routing all agree on how the app is supposed to run.
Output: Updated runtime/env scripts and docs for the final Express-backed contract.
</objective>

<execution_context>
@C:/Users/Dipanshu pandey/OneDrive/Desktop/Office Work/indiantrademartmain-frontback-main/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Dipanshu pandey/OneDrive/Desktop/Office Work/indiantrademartmain-frontback-main/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-cutover-cleanup-and-verification/03-CONTEXT.md
@docs/backend-separation/backend-runtime-target.md
@src/lib/apiBase.js
@vite.config.js
@tools/devAll.js
@package.json
@.env.netlify.example
@README.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Align browser API base and local runtime scripts to final cutover rules</name>
  <files>src/lib/apiBase.js, vite.config.js, tools/devAll.js, package.json</files>
  <read_first>src/lib/apiBase.js, vite.config.js, tools/devAll.js, package.json, docs/backend-separation/backend-runtime-target.md</read_first>
  <action>Finalize how the browser resolves the backend base URL, keep same-origin `/api` behavior where intended, and update dev/build helper scripts so local startup clearly boots the backend and frontend as separate cooperating runtimes. Remove stale assumptions that still treat localhost overrides or legacy mixed startup flows as the default architecture.</action>
  <verify>rg -n "/api|proxy|server/server.js|dev:all" src/lib/apiBase.js vite.config.js tools/devAll.js package.json</verify>
  <acceptance_criteria>
    - browser API base rules are explicit and stable
    - Vite proxy/dev startup still supports the separated backend flow
    - root scripts describe and start the intended backend-first local stack
  </acceptance_criteria>
  <done>The repo runtime behavior matches the backend-separation architecture instead of the legacy mixed setup.</done>
</task>

<task type="auto">
  <name>Task 2: Update env and onboarding docs for separated deployment</name>
  <files>.env.netlify.example, README.md</files>
  <read_first>.env.netlify.example, README.md, docs/backend-separation/backend-runtime-target.md</read_first>
  <action>Update env examples and README setup instructions so developers know which variables belong to the frontend, which belong to the backend, how local startup works after cutover, and what deployment assumptions remain if Netlify adapters are still present temporarily.</action>
  <verify>rg -n "backend|frontend|VITE_API_URL|SUPABASE|dev:all" .env.netlify.example README.md</verify>
  <acceptance_criteria>
    - docs distinguish frontend and backend env ownership clearly
    - README explains the separated local runtime
    - deployment notes mention any temporary adapter assumptions honestly
  </acceptance_criteria>
  <done>Developers can run and deploy the separated runtime without reconstructing migration context from code.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "/api|proxy" src/lib/apiBase.js vite.config.js` returns matches
- [ ] `rg -n "server/server.js|dev:all" tools/devAll.js package.json README.md` returns matches
- [ ] `.env.netlify.example` documents the current separated env assumptions
</verification>

<success_criteria>

- All tasks completed
- Runtime and env wiring match the new backend boundary
- Developer onboarding reflects the separated architecture
  </success_criteria>

<output>
After completion, create `.planning/phases/03-cutover-cleanup-and-verification/03-01-SUMMARY.md`
</output>
